# - import_tasks: repos.yml

- name: install tools
  become: true
  pacman:
    update_cache: yes
    name:
      # cli
      - bat
      - curl
      - git-delta
      - exa
      - direnv
      - fzf
      - fzy
      - git
      - git-lfs
      - gnupg
      - gnuplot
      - htop
      - httpie
      - jq
      - lazydocker
      - lazygit
      - neovim
      - nodejs
      - ripgrep
      - tmux
      - wget
      - wezterm
      - zoxide

