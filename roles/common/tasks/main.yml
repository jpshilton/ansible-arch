- name: install tools
  become: true
  pacman:
    update_cache: yes
    name:
      - bat
      - curl
      - git-delta
      - exa
      - direnv
      - fzf
      - fzy
      - git
      - git-lfs
      - gnupg
      - gnuplot
      - htop
      - httpie
      - less
      - neovim
      - nodejs
      - ripgrep
      - tmux
      - wget
      - wezterm
      - zoxide

