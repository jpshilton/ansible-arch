# - import_tasks: repos.yml

- name: install tools
  become: true
  dnf:
    name:
      # cli
      - bat
      - curl
      - git-delta
      - exa
      - fd-find
      - direnv
      - fzf
      - fzy
      - git
      - git-lfs
      - gnupg
      - gnuplot
      - htop
      - httpie
      - jq
      - lazydocker
      - lazygit
      - neovim
      - nodejs
      - ripgrep
      - tmux
      - wget
      - wezterm
      - zoxide

